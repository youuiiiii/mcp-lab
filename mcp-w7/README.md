# Week 7

React Native Animation assignment.

Updated `app/userList.tsx` from the Week 5 project to render each user card with `react-native-reanimated`. Each user has a different animation duration.
