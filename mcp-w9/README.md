# Week 9

Camera, gallery, and save image implementation using Expo Camera, Image Picker, Media Library, and File System.

Install dependencies:

```bash
npm install
npx expo install expo-camera expo-media-library expo-image-picker expo-file-system
```

Run project:

```bash
npm run start
```
